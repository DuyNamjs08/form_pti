import React from 'react';
import './styles.scss';

Loading.propTypes = {

};

function Loading(props) {
    return (
        <div>
            <h1>loading</h1>
        </div>
    );
}

export default Loading;